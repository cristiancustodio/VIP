@using Microsoft.AspNetCore.Components

<div class="dashboard-widget @Color" title="@Tooltip">
    <div class="widget-bg" style="background-image: url('icons/@Icon');"></div>
    <div class="widget-content">
        <div class="widget-icon">
            <img src="icons/@Icon" alt="@Title icon" />
        </div>
        <div class="widget-info">
            <span class="widget-title">@Title</span>
            <span class="widget-value">@Value</span>
        </div>
    </div>
</div>

@code {
    [Parameter] public required string Title { get; set; }
    [Parameter] public required object Value { get; set; }
    [Parameter] public required string Icon { get; set; }
    [Parameter] public required string Tooltip { get; set; }
    [Parameter] public required string Color { get; set; } = "bg-primary";
}
