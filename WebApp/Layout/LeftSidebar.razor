<div class="sidebar">
    <div class="d-flex flex-column pt-4">
        <NavLink href="/home" class="nav-link" activeClass="active">
            <i class="bi bi-house-door sidebar-icon"></i>
            <span class="sidebar-text">Início</span>
        </NavLink>
        <NavLink href="/patients" class="nav-link" activeClass="active">
            <i class="bi bi-people sidebar-icon"></i>
            <span class="sidebar-text">Pacientes</span>
        </NavLink>
        <NavLink href="/appointments" class="nav-link" activeClass="active">
            <i class="bi bi-calendar-check sidebar-icon"></i>
            <span class="sidebar-text">Agendamentos</span>
        </NavLink>
        <NavLink href="/records" class="nav-link" activeClass="active">
            <i class="bi bi-file-earmark-text sidebar-icon"></i>
            <span class="sidebar-text">Prontuários</span>
        </NavLink>
        <NavLink href="/reports" class="nav-link" activeClass="active">
            <i class="bi bi-graph-up sidebar-icon"></i>
            <span class="sidebar-text">Relatórios</span>
        </NavLink>
        <NavLink href="/settings" class="nav-link" activeClass="active">
            <i class="bi bi-gear sidebar-icon"></i>
            <span class="sidebar-text">Configurações</span>
        </NavLink>
        
        <div class="flex-grow-1"></div>
        
        <NavLink href="/profile" class="nav-link mb-4" activeClass="active">
            <i class="bi bi-person-circle sidebar-icon"></i>
            <span class="sidebar-text">Perfil</span>
        </NavLink>
    </div>
</div>

@code {
    // Parâmetros e métodos de navegação removidos pois NavLink gerencia o estado ativo automaticamente
}